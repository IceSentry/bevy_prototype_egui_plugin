// TODO
// Implement a wgpu painter that paints the app context

use egui::PaintJobs;

pub fn paint_jobs(_jobs: PaintJobs, _texture: &egui::Texture) {
    // self.upload_texture(display, texture);

    // let mut target = display.draw();
    // target.clear_color(0.0, 0.0, 0.0, 0.0);
    // for (clip_rect, triangles) in jobs {
    //     self.paint_job(&mut target, display, clip_rect, &triangles, texture)
    // }
    // target.finish().unwrap();
}
